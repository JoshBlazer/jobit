# Scheduler runs with 3 replicas. Only the instance that wins the etcd
# election is active; the other two are hot standbys.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: pulse-scheduler
  namespace: pulse
  labels:
    app: pulse-scheduler
spec:
  replicas: 3
  selector:
    matchLabels:
      app: pulse-scheduler
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels:
        app: pulse-scheduler
    spec:
      containers:
        - name: scheduler
          image: pulse:latest
          args: ["--role", "scheduler"]
          envFrom:
            - configMapRef:
                name: pulse-config
          resources:
            requests:
              cpu: 50m
              memory: 32Mi
            limits:
              cpu: 200m
              memory: 128Mi
