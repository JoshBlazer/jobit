apiVersion: apps/v1
kind: Deployment
metadata:
  name: Sluice-worker
  namespace: Sluice
  labels:
    app: Sluice-worker
spec:
  replicas: 2
  selector:
    matchLabels:
      app: Sluice-worker
  template:
    metadata:
      labels:
        app: Sluice-worker
    spec:
      terminationGracePeriodSeconds: 60
      containers:
        - name: worker
          image: Sluice:latest
          args: ["--role", "worker", "--shutdown-timeout", "45s"]
          envFrom:
            - configMapRef:
                name: Sluice-config
          lifecycle:
            preStop:
              exec:
                # Give the worker time to drain before the pod is killed.
                command: ["/bin/sh", "-c", "kill -SIGTERM 1 && sleep 5"]
          resources:
            requests:
              cpu: 200m
              memory: 64Mi
            limits:
              cpu: 1000m
              memory: 256Mi
---
# KEDA ScaledObject — scales worker replicas based on Redis queue depth.
# Requires KEDA to be installed in the cluster (https://keda.sh).
apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: Sluice-worker-scaler
  namespace: Sluice
spec:
  scaleTargetRef:
    name: Sluice-worker
  minReplicaCount: 1
  maxReplicaCount: 20
  pollingInterval: 15
  cooldownPeriod: 60
  triggers:
    - type: redis
      metadata:
        address: redis:6379
        listName: queue:1
        listLength: "10"
    - type: redis
      metadata:
        address: redis:6379
        listName: queue:5
        listLength: "10"
    - type: redis
      metadata:
        address: redis:6379
        listName: queue:10
        listLength: "10"
